# Dataset Splits

Suggested split files:

- `train.csv`
- `validation.csv`
- `test.csv`

Each file should reference entries from `data/annotations/` so the team can keep a stable evaluation protocol across members.

